# I don't love yaml, but it's a little easier to format queries in here than in Clojure source
# Use this file by calling (get-queries :basic)
queries:
  basic: |-
    {
      all_artists {
        name
        id
      }
    }
